services:
  - type: web
    name: ai-music-mvp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run_mvp.py --host 0.0.0.0
    healthCheckPath: /api/health
